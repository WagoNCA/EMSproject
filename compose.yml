services:
  
  backend:
    image: backend
    restart: always
    ports:
      - "8080:8080"

  postgres:
    image: postgres
    restart: always

    shm_size: '128mb'
    
    environment:
      POSTGRES_USER: WagoNCA
      POSTGRES_PASSWORD: AdminerVisu
      POSTGRES_DB: PostgreSQL

  influxdb:
    image: influxdb
    restart: always